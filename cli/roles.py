"""Agent role identifiers — three separate agents, never merged."""

ORCHESTRATOR_ROLE = "orchestrator"
PROMPT_CRAFTER_ROLE = "prompt-crafter"
IMAGE_GENERATOR_ROLE = "image-generator"

ALL_ROLES = (ORCHESTRATOR_ROLE, PROMPT_CRAFTER_ROLE, IMAGE_GENERATOR_ROLE)
