# 工程 Spec 增量：IT Shell

## 执行元数据

- **Status**：confirmed（用户 2026-08-03：「it 可以开 shell」）
- **Parent**：IT home-ops + broad-read
- **Created**：2026-08-03

## 目标

IT 可通过 `shell run --command … --i-confirm` 执行本机 shell（管道/通配允许）。

## 边界

- **cwd** 默认仓库根；`--cwd` 仅允许仓库或 `~/.gamefactory`
- 命令字符串本身不限制（用户已授权信任本会话）
- 输出截断 + `sk-` 脱敏
- 策划 brief profile **仍无** shell
- 仍建议优先用 doctor / inspect / conversations / pipeline 专用工具

## 验收

- 白名单含 `shell run`；无 `--i-confirm` 拒绝
- `echo hi` 返回 ok
- brief profile 拒绝 shell
