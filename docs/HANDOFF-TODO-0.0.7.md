# 交接：v0.0.8 已发

> **日期**：2026-07-26  
> **版本**：`gui/package.json` → **0.0.8**

## 本版要点

- DeepSeek 退役 `deepseek-chat` → 自动迁到 `deepseek-v4-flash`（load_config 写回 + 运行时映射）
- 对话滚动位置记忆，取消打开时 smooth 长滚

## 仍开（不挡发版）

- 一整局真人验收 → 可作后续版本
