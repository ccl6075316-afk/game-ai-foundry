# Review：v0.1.5 发版前（brief 结构 + Pi Node + 文档栏工程恢复）

- **Status**：APPROVE
- **Date**：2026-07-31
- **Scope**：工作区未提交变更（scenes/systems/ui_panels、Pi Node 优先、文档栏工程恢复与列表布局）
- **Verification**：`python -m unittest test_pi_runtime test_brief_scenes_systems test_ui_panels test_ui_wireframe -q`；`tsx --test projectPaths.preference.test.ts briefPreviewFormat.test.ts`（均绿）

## 结论

可以打 `v0.1.5` 发版。无阻塞缺陷；下列为已知取舍与残余风险。

## Findings

### Non-blocking

1. **冷启动 `__none__` → `briefs[0]` 回退**  
   `loadInitial` 在无 `lastBrief` / session bind 时用最新 listBriefs。可能把顶栏绑到非用户本意工程（旧「黑哨」类问题的弱化版）。接受原因：避免文档列表空窗；用户可再切工程。

2. **文档列表 `flex-shrink: 0` + 内容区 `min-height: 280`**  
   极矮窗口下侧栏整体滚动，列表约 1.5 card 仍可见。已人工确认「好多了」。

3. **dev 诊断日志**  
   `[docs] listProjectDocs` / main `list-project-docs` 仅非 packaged / DEV，可留。

### Residual risks

- 纯净机无系统 Node 仍走 Electron-as-Node，macOS 正式包可能偶发 Dock 闪一下（dev 已优先系统 Node）。
- fishing `brief.draft.json` 体量大，属产品稿同步，与 Foundry 发版解耦。

## Verdict

**APPROVE** — 进入文档更新 → commit → tag → build → publish → push。
